import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    borderRadius: 8,
    padding: 8,
    justifyContent: "center",
    alignItems: "center",
    width: "fit-content",
    alignItems: "center"
  },
  buttonIcon: {},
  buttonText: {
    paddingVertical: 4,
  }
});