import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    borderRadius: 8,
    padding: 8,
    justifyContent: "center",
    alignItems: "center",
    alignItems: "center"
  },
  buttonText: {
    paddingVertical: 4,
  }
});