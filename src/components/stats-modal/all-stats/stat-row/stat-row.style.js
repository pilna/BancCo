import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  statRowContainer: {
    width: "100%",
    flexDirection: "row",
    justifyContent: "space-around",
    paddingVertical: 10
  }
});