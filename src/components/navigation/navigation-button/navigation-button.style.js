import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  buttonIcon: {
    width: 36,
    height: 36,
    backgroundColor: "red"
  },
  buttonText: {
    marginTop: 4,
    fontSize: 12,
    fontWeight: 'bold',
  }
});